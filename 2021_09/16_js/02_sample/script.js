const numbers = [1, 2, 3];

// define a function
// create a space for input
const calc = (input) => {
  console.log(input);
  // 3 = 1, 2
  const output = input[0] + input[1];
  console.log(output);
};

// call
// pass a value as a input
calc(numbers);
