// time complexity
// O(n)
// space complexity
// O(1)
function booo(n) {
  for (let i = 0; i < array.length; i++) {
    console.log('Booo');
  }
}
booo([1, 2, 3, 4, 5]);

// O(n)
function arrayOfHiNtimes(n) {
  // O(1)
  let hiArray = [];
  // O(n)
  for (let i = 0; i < array.length; i++) {
    // O(n)
    hiArray[i] = 'hi';
  }
  // O(1)
  return hiArray;
}
arrayOfHiNtimes(5);

// O(1)
// depends on language
'skdjfslsjks'.length;
